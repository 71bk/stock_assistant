# Copy to .env.monitoring and fill in. Used only by compose.monitoring.yaml (local).
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=replace-with-a-password
GRAFANA_PORT=3000
# Slack incoming webhook for your #dev channel — local alerts go here.
SLACK_WEBHOOK_DEV=
